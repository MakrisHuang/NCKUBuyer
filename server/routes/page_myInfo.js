var express = require('express');
var router = express.Router();

module.exports = function(app){
    app.get('/myInfo', function(request, response){
        
    });
    
    app.post('/myInfo', function(request, response){
        
    });
};